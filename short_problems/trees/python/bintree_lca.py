#!/usr/bin/python

"""
Given a binary tree and two nodes compute
the least common ancestor of these two nodes
"""

class Node:
    def __init__(self, data, left = None, right = None):
        self.data, self.left, self.right = data, left, right

def lca_inorderV1(root, val1, val2, nodes = []):
    if not root

    if root.val
